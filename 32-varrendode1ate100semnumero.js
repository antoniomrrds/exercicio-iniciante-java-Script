const varrendoNumeros =() => {

const zero = ''
const cem = 'd'.charCodeAt( zero.length)
  
    for (let index = zero.length; index <= cem; index++) {
                      
        console.log(`Número ${index}`)
    }
}
varrendoNumeros();

